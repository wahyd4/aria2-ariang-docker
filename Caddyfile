{$DOMAIN} {
  filebrowser / /data {
    database /root/filebrowser.db
  }
  log stdout
  errors stdout
  gzip
}

{$DOMAIN}/ui {
  root /usr/local/www/aria2
  timeouts none
  gzip
  log stdout
  errors stdout
}
